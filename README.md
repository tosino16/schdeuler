# Job Scheduler Script

This script automates the generation and validation of reports. Each step in the script represents a task that needs to be completed. If a task fails, the script will exit.

## Usage

Run the script:

```bash
./job_scheduler.sh
